#include <bits/stdc++.h>
#define IOS ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define int long long
using namespace std;

void solve(){
    vector<int> vec; 
    set<int> st;
    map<int,int> mp;
    mp[1] = 2;
    mp[2] = 5;
}

signed main(){
	IOS;
	solve();
	return 0;
}
